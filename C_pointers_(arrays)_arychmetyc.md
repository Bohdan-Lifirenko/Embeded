Твоє речення виправлене:
**«Як працює арифметика вказівників у C?»**

Пояснюю просто та коротко.

---

# **Як працює арифметика вказівників у C**

У C при додаванні або відніманні значень від вказівників операції виконуються **не в байтах**, а **в розмірі типу, на який вказує вказівник**.

Тобто:

```
ptr + 1
```

означає **перейти на наступний елемент масиву**, а не збільшити адресу на 1 байт.

Причина: компілятор *знає розмір типу*, на який вказує ptr.

---

## ✔ Приклад: int *

Припустимо:

```c
int *p = (int*)0x1000;
```

На 32-бітній платформі `int` має розмір **4 байти**.

### Що буде:

```c
p + 1
```

→ адреса стане `0x1000 + 4` → **0x1004**

```c
p + 2
```

→ `0x1000 + 8` → **0x1008**

---

## ✔ Приклад: char *

```c
char *p = (char*)0x1000;
```

`char` = 1 байт

```c
p + 1   → 0x1001
p + 5   → 0x1005
```

---

## ✔ Приклад: double *

```c
double *p = (double*)0x2000;
```

`double` = 8 байт

```c
p + 1   → 0x2008
p + 2   → 0x2010
```

---

# **Що дозволено робити з вказівниками**

### ☑ Додавати/віднімати цілі числа

Відбувається множення на sizeof(тип).

### ☑ Віднімати два вказівники одного типу

Повертає кількість елементів між ними.

```c
int a[10];
int *p = &a[1];
int *q = &a[7];

int diff = q - p; // diff = 6
```

---

# **Що НЕ дозволено**

### ❌ Додавати два вказівники

Безглуздо математично.

### ❌ Виконувати арифметику між вказівниками різних типів

Типи мають збігатися.

### ❌ Виходити за межі масиву

Поводження невизначене.

---

# **Головне правило**

Арифметика вказівників **завжди масштабується** на `sizeof(тип)`.

---

Якщо хочеш — можу намалювати ASCII-схему, яка показує як адреси “перескакують” при різних типах.


# **Приклад:**

# 🎯 **Задача**

Знайти **яке фактичне значення буде збережене у вказівнику `p_red_mask_arrey`** в цьому виразі:

```c
unsigned long volatile * p_red_mask_arrey =
        &GPIO_PORTF_DATA_BITS_R[LED_GREEN];
```

де:

```c
#define LED_GREEN (1U << 3)   // тобто 8
```

і

```c
GPIO_PORTF_DATA_BITS_R  ((volatile unsigned long *)0x40025000)
```

Пам’ятаємо, що **в TivaC TM4C123GH6PM DATA_BITS масив використовує bit-specific addressing**, а індекс `[N]` означає:

```
адреса = base_address + (N << 2)   // зсув у 4 байти
```

---

# ✔ Крок 1: Знайдемо LED_GREEN

```
LED_GREEN = (1U << 3) = 0b00001000 = 8
```

---

# ✔ Крок 2: Обчислюємо адресу елемента масиву

```
GPIO_PORTF_DATA_BITS_R[8]
```

Оскільки це bit-specific addressing:

```
адреса = 0x40025000 + (8 << 2)
```

8 << 2 = 32 = 0x20

---

# ✔ Крок 3: Підставляємо

```
адреса = 0x40025000 + 0x20
адреса = 0x40025020
```

---

# 🟢 **Остаточна відповідь**

У вказівнику `p_red_mask_arrey` буде значення:

```
0x40025020
```

Тобто:

```c
p_red_mask_arrey == (unsigned long volatile*)0x40025020;
```

---

# 🧠 Чому саме така адреса?

Бо індекс у DATA_BITS масиві — це **маска біта**, а кожен можливий біт (1,2,4,8,16,…) має власну адресу з кроком 4 байти:

| Маска | Індекс | Адреса                      |
| ----- | ------ | --------------------------- |
| 1     | 1      | base + 0x04                 |
| 2     | 2      | base + 0x08                 |
| 4     | 4      | base + 0x10                 |
| **8** | **8**  | **base + 0x20** → LED_GREEN |
| 16    | 16     | base + 0x40                 |

---

Якщо хочеш — можу пояснити, як запис у `0x40025020` вмикає **саме зелений LED**, не торкаючись інших пінів.
